<%= simple_form_for :message, url: "messages/send_message" do |f| %>
  <%= f.input :message, as: :text, label: false%>
  <%= f.hidden_field :item, :value => @item.id %>
  <%= f.submit "Send message", class: "btn-primary btn float-right" %>
<% end  %>
